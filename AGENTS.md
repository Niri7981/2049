# 2049 开发规范

## 工作范围与文档

- 开始实现前阅读根目录 [plan.md](plan.md)，再阅读当前模块及相关测试。新 App 范围以 plan.md 为准，旧 V0 设计用于理解已有行为。
- 本轮交付仅为文档。后续只有用户要求实现时才开始对应阶段；按阶段完成和验收，不一次性开发未授权功能。
- 用户的新指示优先于本文件。不得把计划、任务列表或示例当成实际付款、部署或推送授权。
- 报告实际完成项、验证结果和未解决限制；不能把历史验收当作当前运行成功的证据。

## 架构边界

- 前端只负责展示、输入和调用本地管理 API。它可以做输入体验校验，但后端必须独立验证。
- 前端不得导入付款、钥匙串、数据库或服务端配置模块；不得构造、签名、发送交易或作出权威额度/授权判断。
- 钱包、额度、授权、付款和交付状态仅由本地后端维护。App 与 MCP 复用同一业务入口，不各写一套逻辑。
- MCP 是薄适配层。Agent 不获得管理额度、关闭策略、导出密钥或任意签名权限。
- HTTP/MCP handler 只负责认证、输入验证、调用服务和映射响应；业务规则位于独立模块，便于测试和替换界面。
- 本地端口只绑定 loopback，并验证客户端身份；管理通道与 Agent 通道分权，防止跨站请求和连接凭据重放。不能用 CORS 或 localhost 代替认证。
- 私钥仅在后端签名器内短暂使用，密钥存放 macOS 钥匙串。公钥配置变更必须同步验证签名来源；不匹配则停止付款。
- 旧网页模型执行器与自建 Paid API 是测试设施，不能默认成为新 App 的产品能力或主网运行依赖。

## 资金与授权规则

- 金额使用币种最小单位的整数；TypeScript 使用 bigint 或已校验的整数字符串。禁止用浮点数比较金额、累计账本或判断额度。
- JSON 接口中的金额用十进制整数字符串并带网络、资产和精度信息；显示字符串由后端生成。不要只靠 `USDC` 等 ticker 识别资产。
- 外部报价先通过运行时 schema 校验，再绑定 API 身份、请求、网络、mint、收款方、金额和有效期。签名前重新核对不可变记录。
- 预算判断、预占和执行权领取必须通过数据库事务或等效原子机制完成，不能采用“先查余额再独立写入”的竞态流程。
- 一次购买只能消费一次授权并产生一个受控付款流程。客户端重试、重复点击、多个 Agent、重启都必须保持同一购买标识。
- 未设置额度、暂停、失效连接、失效授权、错误报价、签名器不匹配时拒绝新付款；不静默降级到绕过策略的路径。
- 用户授权、客户端认证与 Agent 请求是三种不同事实。模型传来的 `approved: true` 或自行重复一个令牌不能证明用户同意。
- 超额批准只绑定当前一笔；长期授权只覆盖当前 API。涨价和关键付款条件变化必须重新判断，不能扩大授权范围。
- 付款与交付分别建模。HTTP 200、交易签名存在、模型说成功，任何单一信号都不足以证明购买完成。
- 提交后超时属于结果未知；保存原凭据、保留预占、查询原交易。不能删除记录、释放未知付款预算或新建交易来“重试”。
- 交付恢复复用原付款凭据，持久保存 3–5 次重试的进度。不能为了恢复响应再次扣款，也不能承诺第三方未提供的恢复能力。
- 暂停、退出与签名/提交的竞态必须有明确顺序：暂停确认后不得开始新的签名提交；已提交交易继续记账或下次恢复。

## 代码组织与可维护性

- 保持 TypeScript strict 和现有 lint 规则。边界输入使用 unknown 加运行时校验；禁止用 any、双重类型断言或关闭检查掩盖问题。
- 使用现有模块和依赖，优先直接、可读的实现。不要为未来多链、多钱包或未知 Agent 引入插件框架和通用抽象。
- 函数、字段和文件按职责命名。新代码不继续引入 Day 编号；旧数据键和文件名迁移必须保留兼容与恢复路径。
- 状态变更使用明确的状态类型和转换函数，不散落字符串判断或多个相互矛盾的布尔值。
- 时间、外部网络与签名器通过必要的依赖边界隔离，使额度、重试、异常和恢复可以确定性测试；不为此引入完整依赖注入框架。
- 为外部调用设置超时、响应大小限制和明确的重定向策略。测试环境本机 API 与生产外部 API 的访问规则分别定义。
- 失败返回稳定错误码、可展示的脱敏说明及关联购买 ID；不要只吞掉异常，也不要直接返回 SDK 原始错误或请求对象。
- 注释解释约束和原因，尤其是资金状态不变量；不逐行复述代码。产品 UI 不展示无助于用户决策的实现细节。

## 数据、配置与日志

- SQLite schema 变更需要显式迁移，验证旧记录可读、进行中付款可恢复；禁止通过删库重建修复付款问题。
- 开发配置、数据库、钥匙串值、支付载荷和模型密钥不进入 Git、前端构建、截图、公开 API 响应或日志。
- 交易 ID、公钥和付款摘要可用于必要的购买展示；签名载荷不通过通用查询接口泄露给 App 或 Agent。
- 不打印整个环境变量、配置对象、SDK 请求或异常。诊断输出采用字段白名单，敏感文件保持受限权限。
- 网络、钱包身份和存储位置要明确。更换钱包或网络时不能把旧记录改写成新钱包记录；先核对进行中付款并设计隔离/迁移。
- 测试币、固定数据和模拟服务必须明确标记；主网配置需独立校验，不能将 Devnet 地址替换为字符串就视为支持主网。

## 测试与完成标准

- 资金、授权、预算、幂等、退出恢复改动必须有有意义的自动化测试，覆盖失败、重放、并发和重启路径，而不只测试成功。
- 修复可复现 bug 时，在实际发生问题的模块边界先增加失败用例，再修复。不要写仅复制实现的测试。
- 普通样式、文案和文档修改不新增无意义测试；按风险验证，避免为低影响改动反复跑全套。
- 单元和集成测试默认不使用真实私钥、不调用收费模型、不付款。模拟响应和 fixture 与真实链上验收证据明确区分。
- 实际 Devnet 购买在对应测试授权范围内进行；主网购买必须有具体 API/收款方、用途和金额上限的授权。
- 代码交付前运行相关测试、`npm run typecheck`、`npm run lint` 和 `npm run build`；涉及 App 壳时补充该平台构建和启动验证。发生失败要报告，不能改成忽略错误。
- 购买链路整合后，按 plan.md 场景执行必要回归；检查前端依赖/构建产物不包含签名实现和敏感配置。
- App 验收包括窗口关闭后台存活、暂停、退出、重新启动和未授权客户端请求；Codex 验收必须使用实际宿主，而不是只有伪造 MCP 调用。
- 完成前移除临时调试代码，检查 diff，无无关重构或敏感文件。报告修改内容、测试证据和限制，不自动提交、推送或发布。
